# This is just an EXAMPLE. Do NOT put your real key in a file that goes to GitHub.
# On Streamlit Cloud, you'll paste your real key into the "Secrets" box in the app settings instead.

HUNTER_API_KEY = "your_hunter_api_key_here"
